import { InlineEditorDirective } from './inline-editor.directive';

describe('InlineEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new InlineEditorDirective();
    expect(directive).toBeTruthy();
  });
});
